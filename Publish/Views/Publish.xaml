<UserControl x:Class="Publish.Views.Publish"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer4BorderBrush}}"
                BorderThickness="2">
            <ContentControl prism:RegionManager.RegionName="Publicar#Region" />
        </Border>

        <DockPanel Grid.Row="1">
            <Button Command="{Binding PublishCommand}"
                    Content="Publicar"
                    FontSize="20"
                    FontWeight="DemiBold" />
        </DockPanel>
    </Grid>
</UserControl>
