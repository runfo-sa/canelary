<UserControl x:Class="Editor.Views.TextEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:control="clr-namespace:Editor.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:model="clr-namespace:Editor.Models"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="HoverMouse"
               TargetType="{x:Type Button}">
            <Setter Property="Foreground"
                    Value="{DynamicResource PrimaryHueDarkForegroundBrush}" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
        </Style>

        <DataTemplate x:Key="HeaderTemplate"
                      DataType="{x:Type model:TabItem}">
            <DockPanel>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseDown">
                        <i:InvokeCommandAction Command="{Binding DataContext.CloseMiddleClickCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                               PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <Grid>
                    <TextBlock DockPanel.Dock="Left"
                               Text="{Binding Header}"
                               ToolTip="{Binding Path}"
                               Margin="5,0"
                               FontSize="12"
                               TextAlignment="Center"
                               VerticalAlignment="Center" />
                    <Thumb Style="{DynamicResource InvisibleThumbStyle}"
                           dragablz:DragablzItem.IsCustomThumb="True" />
                </Grid>
                <Button DockPanel.Dock="Right"
                        Command="{Binding DataContext.CommandService.CloseCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                        CommandParameter="{Binding}"
                        Content="{materialIcons:MaterialIconExt Kind=CloseCircle,Size=14}"
                        Style="{StaticResource HoverMouse}" />
            </DockPanel>
        </DataTemplate>

        <Style TargetType="ToolTip">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border BorderThickness="2"
                                BorderBrush="{DynamicResource SecondaryHueMidBrush}"
                                Background="{DynamicResource PrimaryHueDarkBrush}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground"
                    Value="{DynamicResource PrimaryHueDarkForegroundBrush}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <dragablz:TabablzControl ItemsSource="{Binding TabsList}"
                                 HeaderItemTemplate="{Binding Source={StaticResource HeaderTemplate}}"
                                 SelectedIndex="{Binding CurrentTabIndex}">
            <TabControl.ContentTemplate>
                <DataTemplate DataType="{x:Type model:TabItem}">
                    <control:LinterTextEditor Document="{Binding Content}"
                                              Background="Transparent"
                                              BorderBrush="{DynamicResource SecondaryHueMidBrush}"
                                              BorderThickness="2,0,2,10"
                                              Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                              LineNumbersForeground="{DynamicResource SecondaryHueMidBrush}"
                                              FontFamily="Cascadia Mono Regular"
                                              FontSize="14"
                                              LinterData="{Binding LintingData}"
                                              ShowLineNumbers="True"
                                              ShowLineNumbersWithDottedMargin="False"
                                              SyntaxHighlighting="ZPL"
                                              WordWrap="True">
                        <control:LinterTextEditor.ContextMenu>
                            <ContextMenu>
                                <MenuItem Command="Undo" />
                                <MenuItem Command="Redo" />
                                <Separator />
                                <MenuItem Command="Cut" />
                                <MenuItem Command="Copy" />
                                <MenuItem Command="Paste" />
                            </ContextMenu>
                        </control:LinterTextEditor.ContextMenu>
                    </control:LinterTextEditor>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </dragablz:TabablzControl>

        <Grid Height="256"
              VerticalAlignment="Bottom"
              Visibility="{Binding ShowErrorWindow}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.Background>
                <SolidColorBrush Opacity="0.85" />
            </Grid.Background>

            <DockPanel LastChildFill="False"
                       Margin="15,5">
                <TextBlock FontSize="16"
                           FontWeight="DemiBold"
                           Text="Errores encontrados:" />
                <Button DockPanel.Dock="Right"
                        Background="{x:Null}"
                        BorderBrush="{x:Null}"
                        Command="{Binding CloseErrorWindowCommand}"
                        Content="⤫"
                        FontSize="16"
                        FontWeight="DemiBold"
                        VerticalAlignment="Top" />
            </DockPanel>

            <ScrollViewer Grid.Row="1"
                          Margin="15,0,22,20">
                <TextBlock FontWeight="Normal"
                           FontSize="12"
                           Text="{Binding ErrorsMessage}" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>