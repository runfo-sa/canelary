<UserControl x:Class="Cohere.Views.ProductReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:converters="clr-namespace:Cohere.Converters"
             xmlns:coreDb="clr-namespace:Core.Database.Model;assembly=Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:ProductErrorToForegroundBrushConverter x:Key="PrdouctErrorForegroundColorConverter" />
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding SelectedValues}">
        <ItemsControl.ItemTemplate>
            <DataTemplate DataType="{x:Type coreDb:Valor}">
                <Label FontSize="16"
                       FontWeight="Regular"
                       Margin="10,10">
                    <Label.Style>
                        <Style BasedOn="{StaticResource {x:Type Label}}"
                               TargetType="{x:Type Label}">
                            <Setter Property="Foreground"
                                    Value="{Binding Error, Converter={StaticResource PrdouctErrorForegroundColorConverter}}" />
                        </Style>
                    </Label.Style>

                    <Label.Content>
                        <TextBlock>
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}: {1}">
                                    <Binding Path="Atributo" />
                                    <Binding Path="Valores" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Label.Content>
                </Label>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
