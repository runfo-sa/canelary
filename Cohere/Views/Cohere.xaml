<UserControl x:Class="Cohere.Views.Cohere"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ContentControl prism:RegionManager.RegionName="Cohere#MenuRegion" />

        <Grid Grid.Row="1"
              Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="60*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="30*" />
            </Grid.ColumnDefinitions>

            <ContentControl prism:RegionManager.RegionName="Cohere#TreeRegion" />

            <GridSplitter Grid.Column="1"
                          Background="Transparent"
                          HorizontalAlignment="Stretch" />

            <ContentControl Grid.Column="2"
                            prism:RegionManager.RegionName="Cohere#ProductListRegion" />

            <Grid Grid.Column="2"
                  Visibility="{Binding WarningVisibility}"
                  VerticalAlignment="Center">
                <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}"
                        Background="#323232"
                        BorderThickness="5">
                    <StackPanel>
                        <TextBlock Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                   FontSize="16"
                                   Margin="10"
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Center"
                                   Text="Advertencia: La opcion de Generar Muestra se encuentra deshabilitada.&#x0a;Para habilitarlo se necesita crear una Regla o asignar una ya existente." />
                        <Button Content="Crear Regla"
                                Command="{Binding CommandService.CreateRuleCommand}"
                                Style="{StaticResource MaterialDesignRaisedSecondaryDarkButton}"
                                Margin="10,0"
                                FontSize="16" />
                        <Button Content="Asignar Regla"
                                Command="{Binding CommandService.ChangeRuleCommand}"
                                Style="{StaticResource MaterialDesignRaisedSecondaryDarkButton}"
                                Margin="10"
                                FontSize="16" />
                    </StackPanel>
                </Border>
            </Grid>

            <GridSplitter Grid.Column="3"
                          Background="Transparent"
                          HorizontalAlignment="Stretch" />

            <DockPanel Grid.Column="4"
                       LastChildFill="False">
                <Border DockPanel.Dock="Bottom"
                        BorderBrush="{DynamicResource SecondaryHueMidBrush}"
                        BorderThickness="5">
                    <StackPanel Margin="5,0,5,25">
                        <Label Content="Reporte"
                               FontSize="18"
                               Margin="0,0,0,15" />
                        <TextBlock FontSize="14"
                                   Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                   Text="{Binding ErrorCount, StringFormat='Errores encontrados: {0}'}" />
                    </StackPanel>
                </Border>

                <Button DockPanel.Dock="Bottom"
                        Command="{Binding GenerateSampleCommand}"
                        Content="Generar Muestra"
                        FontSize="18"
                        FontWeight="DemiBold"
                        Margin="0,10,0,15"
                        VerticalAlignment="Bottom" />

                <ContentControl prism:RegionManager.RegionName="Cohere#ProductReportRegion" />
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>