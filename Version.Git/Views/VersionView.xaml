<UserControl x:Class="VersionGit.Views.VersionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <DockPanel LastChildFill="False">
        <TextBlock DockPanel.Dock="Top"
                   FontWeight="DemiBold"
                   Text="{Binding GitTag.Tag, StringFormat='Versión: \{0\}'}"
                   VerticalAlignment="Center" />
        <Button DockPanel.Dock="Right"
                Background="Transparent"
                BorderBrush="Transparent"
                Command="{Binding UpdateCommand}"
                Content="{materialIcons:MaterialIconExt Kind=Update, Size=24}"
                IsEnabled="{Binding NeedsUpdate}"
                ToolTip="Actualizacón pendiente" />
        <TextBlock DockPanel.Dock="Right"
                   VerticalAlignment="Center">
                    <Hyperlink NavigateUri="{Binding GitTagUri}"
                               RequestNavigate="Hyperlink_RequestNavigate">
                        <TextBlock FontWeight="DemiBold"
                                   Text="{Binding GitTag.Message}" />
                    </Hyperlink>
        </TextBlock>
    </DockPanel>
</UserControl>