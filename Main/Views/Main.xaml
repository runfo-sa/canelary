<Window x:Class="Main.Views.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:Main.Controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
        Title="Canelary"
        Height="690"
        Width="1024"
        Style="{StaticResource MaterialDesignWindow}"
        FontFamily="../Resources/#Kumbh Sans Light"
        WindowStartupLocation="CenterScreen">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding CheckUpdatesCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <materialDesign:DialogHost Identifier="UpdateDialog"
                               CloseOnClickAway="False">
        <Grid>
            <controls:SplitView ItemsSource="{Binding ModulesButtons}" />

            <!--
        <Grid Grid.Column="1"
              Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel LastChildFill="False">
                <TextBlock FontSize="26"
                           FontWeight="DemiBold"
                           Text="Estado" />
                <Button DockPanel.Dock="Right"
                        Command="{Binding ChangeThemeCommand}"
                        Content="{materialIcons:MaterialIconExt Kind=ThemeLightDark, Size=24}"
                        ToolTip="Cambiar tema Blanco/Negro" />
            </DockPanel>

            <Border Grid.Row="1"
                    VerticalAlignment="Center">
                <StackPanel Margin="10">
                    <DataGrid AutoGenerateColumns="False"
                              ColumnWidth="*"
                              FontSize="16"
                              FontWeight="DemiBold"
                              IsReadOnly="True"
                              ItemsSource="{Binding ClientsList}">
                        <DataGrid.RowStyle>
                            <Style BasedOn="{StaticResource {x:Type DataGridRow}}"
                                   TargetType="{x:Type DataGridRow}">
                                <Setter Property="Foreground"
                                        Value="{Binding Estado, Converter={StaticResource StatusColorConverter}}" />
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Cliente}"
                                                Header="Cliente" />
                            <DataGridTextColumn Binding="{Binding Estado, Converter={StaticResource StatusNameConverter}}"
                                                Header="Estado" />
                            <DataGridTextColumn Binding="{Binding UltimaConexion}"
                                                Header="Ultima Conexión" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Margin="0,25,0,0"
                                Orientation="Horizontal">
                        <Button Command="{Binding UpdateClientsCommand}"
                                Content="Refrescar"
                                FontSize="14"
                                FontWeight="DemiBold" />
                        <TextBlock FontSize="14"
                                   Margin="10"
                                   Text="{Binding LastRefreshed, StringFormat='Ultimo análisis: hace \{0\} minutos'}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <ContentControl prism:RegionManager.RegionName="Main#VersionRegion"
                            Grid.Row="2" />
        </Grid>
        -->
        </Grid>
    </materialDesign:DialogHost>
</Window>